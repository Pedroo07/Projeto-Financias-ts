import { Category } from '../types/Category'

export const categories: Category = {
    gasto: { title: 'Despesas', color: 'red', expense: true },
    renda: { title: 'Renda', color: 'green', expense: false }
}
